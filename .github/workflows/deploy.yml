# name: Deploy Website to GoDaddy

# on:
#   push:
#     branches:
#       - main  # Deploy when changes are pushed to the main branch

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v3

#     - name: FTP Deploy
#       uses: SamKirkland/FTP-Deploy-Action@4.3.0
#       with:
#         server: ${{ secrets.FTP_HOST }}
#         username: ${{ secrets.FTP_USERNAME }}
#         password: ${{ secrets.FTP_PASSWORD }}
#         local-dir: ./   # Path to the directory to upload
#         server-dir: /public_html/  # Path on the server where files should be uploaded
#         sync-root: /public_html/   # Ensure this matches your server-dir to prevent deletions
#         dangerous-clean-slate: false  # Ensure this is set to false to avoid deleting existing files
